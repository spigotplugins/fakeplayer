---
main: "${project.group}.fakeplayer.FakePlayerPlugin"
name: "${project.projectName}"
version: "${project.version}"
description: "${project.description}"
api-version: 1.13
authors:
- portlek
depend:
  - ProtocolLib
commands:
  fakeplayer:
    description: Main command of the FakePlayer plugin.
    usage: /<command> reload
    permission: fakeplayer.command.main
    aliases:
    - fp
    - fakep
    - fplayer
